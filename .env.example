# Configure these as Sites server environment values before deployment.
# They must never be exposed as NEXT_PUBLIC variables or committed with values.
APP_ORIGIN=https://life-archive-vault.louis-nijholt.chatgpt.site
GOOGLE_DRIVE_CLIENT_ID=
GOOGLE_DRIVE_CLIENT_SECRET=
# Exactly 64 hexadecimal characters (32 random bytes); keep stable across deployments.
GOOGLE_DRIVE_TOKEN_KEY=
